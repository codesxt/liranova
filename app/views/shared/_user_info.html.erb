<%= link_to gravatar_for(current_user, size: 52), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "Ver mi perfil", current_user %>
</span>
<span>
  <%= current_user.publications.count%> publicaciones
</span>
