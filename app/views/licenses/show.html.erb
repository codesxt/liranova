<% provide(:title, @license.name) %>
<div class="row">
  <% if signed_in? %>
  <aside class="span4">
    <section>
	   <%= render 'shared/user_navigation' %>
    </section>
    <section>
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <% end %>
  <% if signed_in? %>
  <div class="span8">
  <% else %>
  <div class="span12">
  <% end %>
      <h1>
        <%= @license.name %>
      </h1>
      <h2>
        <%= @license.description %>
      </h2>
      <%= link_to "Volver a las licencias", licenses_path %>
  </div>
</div>
